{% extends 'base.html.twig' %}

{% block title %}Профайл{% endblock %}

{% block body %}
    <a href="{{ path('logout') }}"><i class="fa fa-lock"></i>Выйти</a>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card mx-4">
                    <div class="card-body p-4">
                        {% include 'flash.html.twig' %}

                        <h3>Профиль</h3>
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <button type="submit" class="btn btn-primary">Сохранить</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}